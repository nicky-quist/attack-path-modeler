__pycache__/
*.pyc
.venv/
*.egg-info/

# Derived caches — regenerated on first run from NVD / EPSS / CISA KEV
data/.cve_cache.json
data/.epss_cache.json
data/.kev_cache.json

# Generated by `python main.py --graph-plot --save-plot`. The risk report
# (attack_path_report.png) stays tracked because the README embeds it; this
# one is a scratch rendering nothing links to.
attack_path_graph.png
